// src/components/ConvertButton.js
import React from 'react';

const ConvertButton = ({ onConvert }) => {
  return <button onClick={onConvert}>Convert to webm</button>;
};

export default ConvertButton;
